name: CD

on:
  workflow_run:
    workflows: ["CI"]   # Must exactly match the name: CI from your CI file
    types:
      - completed

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sh -s -- -b /usr/local/bin
          trivy --version

      - name: Build Docker image
        env:
          MAXMIND_LICENSE_KEY: ${{ secrets.MAXMIND_LICENSE_KEY }}
        run: make build

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: urx-image
          path: urx.tar
          retention-days: 1
      - name: Upload docker-compose file
        uses: actions/upload-artifact@v7
        with:
          name: docker-compose
          path: docker-compose.yml
          retention-days: 7

  deploy-production:
    name: Deploy to Production
    needs: build
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: production
      url: https://urx.ink
    steps:
      - name: Download Docker image
        uses: actions/download-artifact@v8
        with:
          name: urx-image

      - name: Download docker-compose
        uses: actions/download-artifact@v8
        with:
          name: docker-compose

      - name: Connect to Tailnet
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_CLIENT_SECRET }}
          tags: tag:ci  # Adjust this tag as needed for your Tailnet ACLs

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Add host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan avalon >> ~/.ssh/known_hosts

      - name: Deploy to production # If using doppler, Remember to run: doppler setup on the prod server
        run: |
          # Transfer files to production server
          scp urx.tar docker-compose.yml kalairen@avalon:~/apps/urx/

          # SSH into server and deploy
          ssh kalairen@avalon << 'EOF'
            cd ~/apps/urx
            pwd
            docker load < urx.tar
            doppler run -- docker compose up -d
            rm urx.tar
            docker system prune -f
          EOF
